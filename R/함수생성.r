x <-c(1,2,3)
x

mean(x)   # 변수 값의 평균 구하기

max(x)    # 변수 값의 최대값 구하기

min(x)    # 변수 값의 최솟값 구하기

str5 <-c("Hello!", "World", "is", "good!")
str5

paste(str5, collapse =" ")    # "공백"을 구분자로 str5의 단어들 하나로 합치기
paste0(str5, collapse=" ")